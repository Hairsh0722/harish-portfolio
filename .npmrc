; This project has a pre-existing peer-dependency conflict (typescript 7 vs
; react-scripts 5's peerOptional typescript ^3||^4). Install with legacy peer
; resolution so `npm install` (locally and on the host/CI) succeeds without
; passing --legacy-peer-deps every time.
legacy-peer-deps=true
